// const { createClient } = require("redis");

// const client = createClient({
//     url: 'redis://localhost:6379' // Redis URL with default localhost and port 6379

// });

// client.on("error", (err) => {
//     console.error("Redis Client Error:", err);
// });

// (async () => {
//     await client.connect();  // Ensures connection is established when the application starts
// })();

// module.exports = client;
